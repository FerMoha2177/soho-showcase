
// config/settings.js

const settings = {
    context: '2d',
    animate: true,
    //p5: true,
    dimensions: [1200, 800],
    // Optional: For future audio reactivity
    scaleToView: true,
    // playbackRate: 'throttle'
  };
  
  module.exports = { settings };